const USDollar = new Intl.NumberFormat("en-US", { style: "currency", currency: "USD" });
const VNDong = new Intl.NumberFormat("vi", { style: "currency", currency: "VND" });

export { USDollar, VNDong };
